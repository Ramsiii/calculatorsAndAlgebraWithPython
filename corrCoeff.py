# TODO: creat a calculator that takes a list of tuples with x, y coordinates
# and calculates the correlation coeefficient of these data points
# and then calculate the coefficient of determinitation R² (r**2)
#


# alternatively us numpy:
# import numpy as np
# coords = np.array([[1,2], [3,4], [5,6]])
# correlation = np.corrcoef(coords[:,0], coords[:,1])[0,1]
